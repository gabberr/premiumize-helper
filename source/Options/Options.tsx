import React from 'react';

import './styles.scss';

const Options: React.FC = () => {
  return (
    <div>
      Right click on torrent file or magnet link to show Add to Premiumize to
      showContext menu works on link elements.
    </div>
  );
};

export default Options;
